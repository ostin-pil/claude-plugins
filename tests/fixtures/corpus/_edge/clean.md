# A clean document

This file has no structural tells. It uses plain punctuation, commas, and parentheses (like this) instead of the patterns the scanner flags, and each paragraph is a single soft-wrapped line so the hard-wrap heuristic stays quiet. Nothing here should be reported and the total flagged count must be zero.

A short closing line keeps the file realistic.
