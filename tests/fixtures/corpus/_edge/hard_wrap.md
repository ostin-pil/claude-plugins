# Hard-wrapped paragraph

This paragraph has been deliberately hard-wrapped into short lines
that each sit comfortably inside the fifty to ninety-five character
band the scanner uses to detect manual line breaks inside what
should be one soft-wrapped paragraph for the markdown renderer.

This second short prose line follows another short prose line and
so the pair trips the hard-wrap heuristic on its second member.
