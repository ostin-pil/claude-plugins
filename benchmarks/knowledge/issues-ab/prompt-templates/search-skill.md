You are searching a project's issue tracker, as a benchmark of the `issues`
skill. Follow the skill instructions appended below, verbatim. Resolve the
manifest keys from the repo's `.claude/lifecycle-manifest.md`.

Repo: {{REPO}}

Task: run `/issues search thermal throttling`. Find the issues related to
"thermal throttling" and report them.

cd into the repo. Report the matching issues you find, with their IDs and status.
If there are none, say so.
