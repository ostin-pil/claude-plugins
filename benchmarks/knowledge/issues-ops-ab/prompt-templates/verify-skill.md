You are maintaining a project's issue tracker, as a benchmark of the `issues`
skill. Follow the skill instructions appended below, verbatim. Resolve the
manifest keys from the repo's `.claude/lifecycle-manifest.md`.

Repo: {{REPO}}

Task: run `/issues verify`. Check the issues marked for verification and update
the tracker accordingly.

cd into the repo and actually update the tracker file (this is not a dry
description). Report what you changed and why.
