{
  "ecosystem": "python-pyproject",
  "product_name": "sprocket",
  "fields": {
    "build_commands": { "accept": [[], ["python -m build"], ["none"]], "note": "Python often has no build step; an empty list or 'none' is the correct answer, not an invented one." },
    "test_commands": { "accept": [["pytest"], ["python -m pytest"]] },
    "code_globs": { "accept_ext": ["py"] }
  },
  "notes": "pyproject with pytest configured. The discriminating case: a correct adoption should NOT invent a build command. test = pytest; code is *.py."
}
