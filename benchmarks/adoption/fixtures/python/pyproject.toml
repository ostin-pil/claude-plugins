[project]
name = "sprocket"
version = "0.1.0"
description = "A small Python sprocket package."
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
